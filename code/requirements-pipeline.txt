# Full training / suite / backtest stack.
# Dashboard heavy jobs probe these imports via dashboard.jobs.util.pipeline_python.
# Install into the chosen interpreter, e.g.:
#   .venv_dashboard/bin/pip install -r requirements-pipeline.txt
#
# Required (suite fails to import without these):
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
optuna>=3.0
catboost>=1.2
tqdm>=4.65
matplotlib>=3.7
scipy>=1.10
#
# Optional (scored as bonus; lightgbm only used when use_lightgbm_base=True):
requests>=2.28
xgboost>=1.7
lightgbm>=4.0
nba_api>=1.4
